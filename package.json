{
  "name": "realtime-chat-app",
  "version": "1.0.0",
  "description": "A real-time chat application using Socket.IO for instant messaging with low latency bidirectional communication",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm run copy-files && npm run optimize",
    "copy-files": "node scripts/build.js",
    "optimize": "echo \"Build optimized for production\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "chat",
    "socket.io",
    "real-time",
    "websocket",
    "messaging"
  ],
  "author": "TechNest Intern",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "express-rate-limit": "^6.11.2",
    "helmet": "^7.2.0",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
